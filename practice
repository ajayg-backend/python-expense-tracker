def recommend_pet_food(species: str, age: int) -> str:

    if species.lower() == "dog":

   
        if age < 2:
            return "Puppy food"
        elif age <= 7:
            return "Adult dog food"
        else:
            return "Senior dog food"
    
    elif species.lower() == "cat":
          return "unknown pet species"



